# J81 WiFi over PCIe: focused plan

Date: 2026-09-13

Target: iPad Air 2 Wi-Fi, J81/J81AP, A8X/T7001

Status: **evidence-gathering phase complete, no code written yet.** Real
J81 ADT evidence for the `apcie` PCIe host controller is now fully
captured and cross-validated (including against this project's own
prior J82-derived predictions, which all confirmed correct). A
structurally-relevant reference driver has been found and pulled.
Mainline Linux's own Apple PCIe driver is confirmed to be the wrong SoC
generation. What remains is precise register-map decoding, then a real
compile-only driver port -- staged, isolated-payload work matching this
project's own established discipline (see "Basic guidelines" below).
**No hardware has been touched.**

## Why WiFi, now

Chosen over continuing the touch investigation: touch input has no UI to
exercise yet on this project (no display/desktop environment), so
finishing TOUCH-2/3 wouldn't be independently useful in the near term.
Network access is useful on its own, even from a plain console/SSH
environment -- and unlike touch's remaining KLCT register-offset mystery
or Bluetooth's PMU write-protocol mystery, WiFi has **no equivalent
mystery**: the chip identity, protocol and Linux driver all already
exist and are well understood. The open work is host-controller
bring-up, not reverse-engineering.

## What this project already knew, before today

`research/driver-gap.md`, `research/feasibility.md` and
`research/hardware.md` already established, from the **public J82 ADT**
(a sibling board, not this project's own captured evidence):

- WiFi is **BCM4350 over T7000 PCIe port 1**, not BCM4354 over SDIO --
  this explicitly *invalidated* an earlier, wrong SDIO-based plan.
  UART2 is a control/sideband path only, not a data bus.
- Predicted resources: PCIe port 1 at 2.5 GT/s, wake GPIO165, CLKREQ
  GPIO174, PERST GPIO179, a DART at `0x602002000`/IRQ216.
- `brcmfmac`'s PCIe support (including BCM4350) already exists in
  Hoolock's kernel tree, just not enabled in the current config
  (`CONFIG_CFG80211`/`BRCMFMAC`/`BRCMFMAC_PCIE` all off).
- The real prerequisite is the T7000 PCIe **host controller** itself --
  nothing in the current kernel or DTS describes it at all.

## Real J81 ADT evidence, 2026-09-13 -- confirms every J82 prediction

Read directly from this project's own private capture
(`artifacts/adt/20260908T082112Z-j81.adt`, git-ignored). All of the
following is from the real board, not inferred from J82.

### `apcie` (the PCIe root complex node)

| Property | Value | Notes |
| --- | --- | --- |
| `compatible` | `apcie,t7000` | Exact match to what the driver-gap research already expected |
| `#ports` | `4` | Matches the reference driver's `NUM_PORT` assumption (see below) |
| `dev-range` | `[0, 0xff]` | Standard PCI device range |
| `interrupts` | `[0xd4, 0xd7, 0xda, 0xdd]` (212, 215, 218, 221) | One IRQ per port |
| `msi-address` | `0xbffff000` | MSI doorbell |
| `#msi-vectors` | `0x20` (32) | Matches the reference driver's `NUM_MSI` |
| `msi-vector-offset` | `0xe0` (224) | |
| `bus-range` | `[0, 4]` | |
| `power-gates` | `0x39` (57) | PMGR power-gate index for the controller |
| `clock-gates` | `[0x39, 0x3a, 0x38]` (57, 58, 56) | Three gates -- **cross-validated**: these numbers match the `PCIE`/`PCIE_AUX`/`PCIE_REF` names found independently in the real iOS 8.1 kernelcache's idle-power-accounting table (see `research/j81-long-term-subsystems.md`'s WiFi note) |
| `apcie-config-tunables` | 12-word blob (`0x90, 0xff, 0x28, 0x130, 3, 3, 0x134, 1, 1`) | Real PHY/link tuning parameters for *this* board -- exactly the "A8X ADT tunables" the driver-gap research already said would be needed instead of copying another chip's values |
| `dbi-overrides` | present, multi-word | DesignWare-style internal config-space register overrides for controller init |
| `no-l1-config-access`, `no-l2-config-access` | present (boolean) | Config-space access quirks the driver needs to respect |
| `function-debug_gpio` | OIPG pin 0x6b (107) | Debug/status GPIO, not required for basic bring-up |

### `pci-bridge1` (child of `apcie`, the actual populated port)

| Property | Value | Notes |
| --- | --- | --- |
| `apcie-port` | `1` | Confirms port 1, as J82 predicted |
| `maximum-link-speed` | `1` | Gen 1, 2.5 GT/s -- **matches the J82 prediction exactly** |
| `function-device_wake` | OIPG pin **0xa5 = 165** | **Matches J82's "wake GPIO165" exactly** |
| `function-clkreq` | OIPG pin **0xae = 174** | **Matches J82's "CLKREQ GPIO174" exactly** |
| `function-perst` | OIPG pin **0xb3 = 179** | **Matches J82's "PERST GPIO179" exactly** |
| `t-refclk-to-perst` | `0x64` (100) | Timing parameter (unit not yet confirmed -- likely µs or ms) |
| `msi-vector-base` | `8` | This port's own slice of the shared 32 MSI vectors (32 / 4 ports = 8 each) |
| `function-dart_force_active` | phandle `0x77` (= `dart-apcie1`), magic `tcaF` | Force-activates the DART before the port comes up -- same `function-*`/magic-cookie platform-function pattern as touch's KLCT, but self-documenting via its property name this time |

### `wlan-pcie,bcm4350` (grandchild of `apcie`, under `pci-bridge1`)

Confirms the actual WiFi endpoint: `compatible = "wlan-pcie,bcm4350",
"wlan-pcie,bcm"`, `device_type = "pcie-device"`, `iommu-parent = 0x78`
(→ `mapper-apcie1`, below), plus PCIe tuning properties
(`pci-aspm-default`, `pci-max-latency`, `pci-l1pm-control`,
`~cpie-l1ss-workaround`). The real `wlan.*` properties elsewhere in this
node's parent scope (`wlan.background-scan-cache`,
`wlan.tx-power-cap-ctrl`, `wlan.bss.5GHz-preference`, `wlan.awdl.params`,
etc.) are BCM4350 firmware/NVRAM configuration -- not needed for basic
bring-up, but real data if calibration is ever needed later.

### `dart-apcie1` / `mapper-apcie1` (the IOMMU this PCIe port uses)

| Property | Value | Notes |
| --- | --- | --- |
| `compatible` | `dart,t7001`, `dart,s5l8960x` | Real Linux DT binding-shaped compatible strings |
| `interrupts` | `0xd8` (216) | **Matches the J82-predicted IRQ216 exactly** |
| `page-size` | `0x1000` (4 KB) | Standard |
| `vm-size` | `0x3ffff000` | |
| `stt-idx-width` | `0xa` (10) | |
| `reg` | one region: addr-offset `0x02002000`, size `0x00200000` (2 MB) | Same encoding convention as `apcie`'s own `reg` below -- see "Register windows" |

`mapper-apcie1` is the DART's mapper child (`reg = 0`, straightforward).

### Register windows: `apcie`'s 12-entry `reg` property, decoded

`apcie`'s `reg` property has **12 entries**, not the single
address+size pair most other nodes in this project have had. Decoded
carefully -- verified the parsing method first against `spi3`'s already
publicly-proven-correct encoding before trusting it on new data, then
cross-checked the same 4-word-per-entry, "word 1 = constant type/flag"
shape against `dart-apcie1`'s own (single-entry) `reg`, which uses the
exact same convention:

```
entry  0: offset=0x10000000  type=0x6  size=0x01000000 (16 MB)   -- config space (ECAM-shaped)
entry  1: offset=0x01004000  type=0x6  size=0x00001000 (4 KB)    -- port 1, region A
entry  2: offset=0x01001000  type=0x6  size=0x00001000 (4 KB)    -- port 1, region B
entry  3: offset=0x02004000  type=0x6  size=0x00001000 (4 KB)    -- port 2, region A
entry  4: offset=0x02001000  type=0x6  size=0x00001000 (4 KB)    -- port 2, region B
entry  5: offset=0x03004000  type=0x6  size=0x00001000 (4 KB)    -- port 3, region A
entry  6: offset=0x03001000  type=0x6  size=0x00001000 (4 KB)    -- port 3, region B
entry  7: offset=0x04004000  type=0x6  size=0x00001000 (4 KB)    -- port 4, region A
entry  8: offset=0x04001000  type=0x6  size=0x00001000 (4 KB)    -- port 4, region B
entry  9: offset=0x00000000  type=0x6  size=0x00002000 (8 KB)    -- shared? (address 0 is unusual)
entry 10: offset=0x01005000  type=0x6  size=0x00001000 (4 KB)    -- port 1, region C
entry 11: offset=0x03005000  type=0x6  size=0x00001000 (4 KB)    -- port 3, region C
```

Clean, obviously-intentional structure: one large config-space region,
then per-port-indexed sub-regions (the port number literally appears in
the offset, e.g. `0xN004000`/`0xN001000`/`0xN005000` for port `N`) --
**but only ports 1 and 3 have a third ("region C") sub-region**, which
is worth understanding rather than assuming is a transcription artifact.
The `type=0x6` field is constant across every entry in both this node
and `dart-apcie1`'s, so it's a real, uniform region-attribute flag, not
an address-high word -- exact meaning not yet decoded, but since it
never varies it doesn't block understanding *which* region is which.

**Not yet resolved**: these offsets are far too small (all well under
16 MB) to be complete 36-bit SoC physical addresses the way every other
device in this project's ADT has been (e.g. `spi3` at `0x20a08c000`).
`apcie` also has its own `ranges` property (a real, present, not-yet-
decoded 6-or-7-cell PCI bus `ranges` translation) which almost certainly
holds the actual base address these offsets are relative to. Decoding
`ranges` correctly is the most important immediate next step (see
guidelines below) -- do not guess at the absolute addresses before that.

## Reference driver research

- **Mainline Linux's `pcie-apple.c`** (`drivers/pci/controller/pcie-apple.c`,
  already in Hoolock's pinned kernel source): confirmed wrong generation.
  Its `of_device_id` table only lists `apple,t6020-pcie` and
  `apple,pcie` (T8103/M1-baseline) -- nothing for `apcie,t7000` at all.
  Not usable even as a loose skeleton without heavy rework; this
  project's own driver-gap research already flagged this as only the
  "preferred integration point *if* its controller model fits" -- it
  doesn't.
- **Corellium's `pcie-hx.c`** (`linux-sandcastle`, `sandcastle-5.4`
  branch, `drivers/pci/controller/pcie-hx.c`, 873 lines, fetched and
  read this session): `compatible = "hx,pcie-h9p"` -- a real, working,
  open-source driver for the *structurally* related older-generation
  Apple PCIe controller family (H9P, a different but related SoC
  generation to T7001/A8X). Confirms the overall shape: `NUM_PORT = 4`,
  `NUM_MSI = 32` (both match our real `#ports`/`#msi-vectors` exactly),
  a `mmio[4 + NUM_PORT]` register-window layout (1 config + 3 shared PHY
  + `NUM_PORT` per-port regions), GPIO-based PERST/CLKREQ via
  `devm_gpiod_get_index`, and its own lightweight "SART" IOMMU
  mechanism (simpler than a full DART). This project's own driver-gap
  research already correctly flagged that "its H9P hard-coded tunables
  cannot be reused on A8X" -- confirmed: T7001 needs its own
  `apcie-config-tunables` (now captured above), and T7001's real
  register layout (12 windows, per-port-indexed) differs in detail from
  H9P's hardcoded 1+3+4 grouping, so this is a **structural guide, not
  a drop-in port**. T7001 also uses a real DART (already has a DART
  driver family in this project's kernel, since ANS1 work already uses
  `drivers/soc/apple` mailbox/DART-adjacent infrastructure), not H9P's
  simpler SART -- another real difference to account for, not copy
  blindly.

## What's solid vs what's still open

**Solid** (real ADT evidence, most of it independently cross-validated):
compatible string, port count, IRQ numbers, DART presence and its own
IRQ/compatible/page-size, the wake/CLKREQ/PERST GPIO numbers and link
speed (all matching prior J82-based predictions exactly), the PMGR
power/clock gate indices (cross-validated against real firmware strings
found separately today), the real per-board PHY tuning blob, and the
12-entry register-window count and relative structure.

**Still open**:
1. `apcie`'s `ranges` property -- needed to get real absolute physical
   addresses for the 12 register windows. Present in the ADT, not yet
   decoded.
2. Which of the 12 windows is genuinely "config" vs "PHY" vs "port
   control" vs the two extra ports-1-and-3-only regions -- needs
   cross-referencing against `pcie-hx.c`'s actual register *usage*
   (`REG_PORT_LTSSMCTL`, `REG_PHY0_PORTSTAT`, etc.), not just its
   window count.
3. Whether Hoolock's kernel already has a usable, generic Apple DART
   driver this port can attach to (very likely yes, given other Apple
   SoC subsystems in this same kernel tree use DART/IOMMU already) --
   not yet confirmed for *this* DART compatible string specifically.
4. `t-refclk-to-perst`'s unit (µs vs ms) -- affects real timing in the
   port bring-up sequence.
5. `apcie`'s `dbi-overrides` and `link-state-power` blobs -- present,
   real, not yet decoded; likely needed for correct link training but
   not necessarily for a first compile-only skeleton.

## Basic guidelines: what needs to be done, in order

Matching this project's own established discipline throughout (evidence
before code, isolated non-default payloads before touching the working
default, compile-verify before hardware, hardware tests as their own
separate explicit decision):

1. **Decode `apcie`'s `ranges` property precisely** (a small, bounded,
   scriptable task -- same rigor as the `reg` decode above: verify the
   parsing approach against a known-correct case first). This resolves
   open item 1 and unblocks everything downstream that needs real
   physical addresses.
2. **Map each of the 12 register windows to its real purpose** by
   reading `pcie-hx.c`'s actual register offsets/usage against our
   window sizes and per-port indexing, not just matching window counts.
   Write this mapping down explicitly before using it in code.
3. **Check Hoolock's kernel tree for existing Apple DART driver
   support** usable by `dart,t7001`/`dart,s5l8960x` -- if a generic
   driver already binds this compatible string, the DART side of this
   port may need little or no new code, only a DT node.
4. **Write the DT node** (`t7001.dtsi`: an `apcie` node using the real,
   now-fully-decoded resources; `t7001-air2.dtsi`: enable it and the
   `wlan-pcie,bcm4350` child) -- **disabled/inert by default**, following
   the exact same pattern already proven for UART3/UART5/SPI3 (added to
   the SoC-level dtsi as disabled, enabled only at board level, and even
   then only wired into an isolated test payload, never the default
   `kernel/hoolock.nix`/`m1n1-hoolock-control`).
5. **Port a compile-only driver skeleton**, structurally adapted from
   `pcie-hx.c` (port count, MSI count, and register layout corrected to
   T7001's real numbers from steps 1-2; DART instead of SART) -- as its
   own dedicated Nix package/payload, exactly the pattern
   `kernel/hoolock-ans1-test.nix` already established for ANS1: never
   touch the default payload, cross-build-verify in isolation first.
6. **Cross-build verify only.** No hardware test at this stage --
   PCIe/DART bring-up is unproven, higher-complexity code; a hardware
   attempt is its own separate, later, explicit decision, same as every
   other risky subsystem in this project (ANS1, the Bluetooth PMU
   write).
7. **Only once the host enumerates on real hardware**: enable
   `CONFIG_CFG80211`/`CONFIG_BRCMFMAC`/`CONFIG_BRCMFMAC_PCIE` (already
   present in Hoolock's kernel source, just off in the current config),
   and extract `brcmfmac4350-pcie` firmware/NVRAM locally from a real
   IPSW (technique already proven and documented this session -- see
   the touch-clock TOUCH-3 sections) -- never commit firmware/NVRAM to
   Git, matching this project's existing rule for all proprietary
   binary data.

No hardware investment is needed for any of steps 1-6; this is pure
software/research work using data and tools already in hand.

## Primary sources

- This project's own prior research:
  [`research/driver-gap.md`](../../research/driver-gap.md)'s "Wi-Fi: PCIe host work precedes brcmfmac",
  [`research/feasibility.md`](../../research/feasibility.md),
  [`research/hardware.md`](../../research/hardware.md)
- [Linux Apple PCIe (mainline, wrong generation)](https://github.com/torvalds/linux/blob/master/drivers/pci/controller/pcie-apple.c)
- [Corellium old-Apple PCIe (`pcie-hx.c`, structural reference)](https://github.com/corellium/linux-sandcastle/blob/sandcastle-5.4/drivers/pci/controller/pcie-hx.c)
- [Linux brcmfmac PCIe](https://github.com/torvalds/linux/blob/master/drivers/net/wireless/broadcom/brcm80211/brcmfmac/pcie.c)
- [SoMainline J82 ADT (public sibling-board reference)](https://github.com/SoMainline/adt_collection/blob/master/a8/J82.adt)
- This project's own private capture:
  `artifacts/adt/20260908T082112Z-j81.adt` (git-ignored, contains device
  serial -- never commit)
